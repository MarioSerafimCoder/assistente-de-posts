import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { AppHeader } from "@/components/layout/AppHeader";
import { brands } from "@/brands";

export default function BrandsPage() {
  return <div className="flex h-full flex-col bg-[#151815] text-white"><AppHeader/><main className="mx-auto w-full max-w-6xl flex-1 overflow-y-auto px-6 py-10"><p className="app-kicker">Brand Manager</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.04em]">Marcas instaladas</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-white/45">Audite logos, cores, tipografia, elementos, imagens e templates. Configurações demonstrativas são identificadas como placeholders.</p><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{brands.map((brand) => <article key={brand.id} className="rounded-2xl border border-white/10 bg-white/[.035] p-5"><div className="flex items-start justify-between"><div><h2 className="text-xl font-semibold">{brand.name}</h2><p className="mt-1 text-xs text-amber-200/70">{brand.logos.primary.placeholder ? "Identidade demonstrativa" : "Identidade oficial"}</p></div><div className="flex gap-1">{[brand.colors.primary, brand.colors.secondary, brand.colors.accent, brand.colors.light].map((color, index) => color && <span key={index} className="h-5 w-5 rounded-full border border-white/15" style={{ background: color }}/>)}</div></div><dl className="mt-7 grid grid-cols-2 gap-y-3 text-sm"><div><dt className="text-white/35">Logos</dt><dd>{Object.keys(brand.logos).length}</dd></div><div><dt className="text-white/35">Cores</dt><dd>{Object.values(brand.colors).filter((value) => typeof value === "string").length}</dd></div><div><dt className="text-white/35">Elementos</dt><dd>{brand.assets.length}</dd></div><div><dt className="text-white/35">Imagens</dt><dd>{brand.images.length}</dd></div><div><dt className="text-white/35">Templates</dt><dd>{brand.templates.length}</dd></div></dl><Link className="editor-action mt-6 w-full justify-between" href={`/settings/brands/${brand.slug}`}>Ver identidade<ArrowRight size={14}/></Link></article>)}</div></main></div>;
}
